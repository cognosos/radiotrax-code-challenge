@import "~stylesheets/variables.scss";

.root {
  width: 100%;

  // 1:1 proportionality trick with height and padding
  height: 0;
  padding: 1em 0;
  padding-bottom: 100%;

  margin: 0;
  text-align: center;
}

.item {
}

.active {
  background-color: adjustLightness(var(--theme-primary), 0.3);
  margin: 0;
}

.icon {
  margin-left: 20px;
  height: 30px;
  width: 30px;
}

.text {
  margin: 0.25rem 0 0 0;
  font-size: 10px;
  text-align: center;
}
